# PlatformIO
.pio/
.pioenvs/
.piolibdeps/
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch/

# ── SECRETS — never commit real credentials ──────────────────────────────────
# config.h holds your WiFi password + Spotify client secret + refresh token.
# Only the template (config.example.h) is tracked.
src/config.h

# OS / editor cruft
Thumbs.db
.DS_Store
*.swp

# Editor plugin caches
.impeccable/
